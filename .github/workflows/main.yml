name: deploy

on:
  push:
    branches:
        - main

  workflow_dispatch:


jobs:
  deploy-to-vercel:
    runs-on: ubuntu-latest
    
    steps:
      - name: mensaje de bienvenida
        run: echo "hola desde githnub actions"  
